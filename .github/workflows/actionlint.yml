name: Actionlint

on:
  push:
    paths:
      - ".github/**"

jobs:
  actionlint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: raven-actions/actionlint@v2
        with:
          pyflakes: false
